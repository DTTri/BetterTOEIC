import { createTheme } from "@mui/material";

const adminTableTheme = createTheme({
  palette: {
    primary: {
      main: "#00205C",
    },
    secondary: {
      main: "#f50057",
    },
  },
});

export default adminTableTheme;
